# Plan: <title>  (from spec)

Status: draft | accepted

The output of plan mode, committed before `implement` runs. This is the resume
point: a fresh session reads this instead of re-deriving the approach.

## Files that change
<Path per line, (new) where created. The blast radius at a glance.>

## Order of work
<Numbered. Each step independently reviewable and, ideally, independently
committable. This doubles as the task list — no separate tickets.>
1.
2.
3.

## Risks
<What could break. The single riskiest step, and the alternatives not taken.>

## Proof
<Which tests prove it and what "done" looks like — command output, a passing
test file, a screenshot matching the mock. Ties back to spec requirements.>

## Links
Intent: sdlc/features/<slug>/intent.md
Spec:   sdlc/features/<slug>/spec.md
