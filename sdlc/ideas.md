# Ideas inbox

Raw concepts, pre-intent. Jot anything here — a sentence is fine. No structure
required. When one is ripe, run `/intent` to interview it into an `intent` issue,
then delete the entry here.

This is scratch, not a record. The record starts at the `intent` issue.

---

<!-- ## <one-line idea> — YYYY-MM-DD
     a line or two of context: what's annoying, who feels it, a rough shape. -->
- We need to clean up old PRs, github issues, and clean the working process of old content
- Put sign in with magic link behind a click. Encourages people to use sign in with google
- Fix mobile view (some work has been attempted with this, but it is incomplete)
- Wikipedia article link/preview
- New map projections (new UN map)
- My answers page has basic stats (%correct, %incorrect, %skipped)
- Fix zoom on countries
- Fix environment indicator “unknown” on localhost
