# Deployment

**Stub.** Nothing here yet.

How this ships and runs: packaging, platform, distribution, update mechanism.

One known open thread: whether packs are bundled with app builds or downloaded at runtime. It affects nothing in the pack format — see [../packs/](../packs/) — but it decides a real part of the app architecture.
