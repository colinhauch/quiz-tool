import { Placeholder } from "./Placeholder.js";

/** Generator Preview surface — what each pack's generator emits (#139). */
export function GeneratorPreview() {
  return <Placeholder title="Generator Preview" />;
}
