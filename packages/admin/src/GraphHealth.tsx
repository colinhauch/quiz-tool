import { Placeholder } from "./Placeholder.js";

/** Graph Health surface — orphans, uncovered statements, coverage gaps (#138). */
export function GraphHealth() {
  return <Placeholder title="Graph Health" />;
}
