import { Placeholder } from "./Placeholder.js";

/** Results surface — all answers, filters, charts, leaderboard (#143, #144). */
export function Results() {
  return <Placeholder title="Results" />;
}
